import SignupForm from '../../components/auth/SignupForm';

const Signup = () => {
    return (
      <div>
        <h3>create new account</h3> {/* Add some content to render */}
        {<SignupForm />}
      </div>
    );
  };
  
  export default Signup;